"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {BookOpen,Brain,CalendarDays,FileText,GraduationCap,Home,LineChart,LogOut,Menu,X} from "lucide-react";import {useState} from "react";import {createClient} from "./supabase";
const items=[['Dashboard',Home,'/dashboard'],['AI Tutor',Brain,'/tutor'],['Quiz',BookOpen,'/quiz'],['Catatan',FileText,'/notes'],['Tugas',CalendarDays,'/tugas'],['Progress',LineChart,'/progress']] as const;
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const[open,setOpen]=useState(false);async function out(){await createClient().auth.signOut();location.href='/login'}return <main className="min-h-screen bg-[#08090d]"><header className="sticky top-0 z-40 border-b border-white/5 bg-[#08090d]/85 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6"><div className="flex items-center gap-3"><button onClick={()=>setOpen(!open)} className="rounded-xl p-2 lg:hidden">{open?<X size={20}/>:<Menu size={20}/>}</button><Link href="/dashboard" className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-fuchsia-500"><GraduationCap size={18}/></span><b>Bantu<span className="text-violet-400">Pelajar</span></b></Link></div><button onClick={out} className="rounded-xl p-2 text-zinc-400 hover:bg-white/5 hover:text-white"><LogOut size={18}/></button></div></header><div className="mx-auto flex max-w-7xl"><aside className={`${open?'fixed inset-x-4 top-20 z-30':'hidden'} glass rounded-2xl p-2 lg:sticky lg:top-20 lg:block lg:h-[calc(100vh-5rem)] lg:w-60 lg:rounded-none lg:border-0 lg:bg-transparent lg:p-4`}>{items.map(([label,Icon,href])=><Link key={label} href={href} onClick={()=>setOpen(false)} className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold ${path===href?'bg-violet-500/10 text-violet-300':'text-zinc-500 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{label}</Link>)}</aside><section className="min-w-0 flex-1 px-4 py-7 sm:px-6 lg:px-8">{children}</section></div></main>}
export function PageTitle({eyebrow,title,desc}:{eyebrow?:string,title:string,desc?:string}){return <div><p className="text-sm font-bold text-violet-400">{eyebrow||'BANTUPELAJAR'}</p><h1 className="mt-2 text-3xl font-extrabold tracking-tight sm:text-4xl">{title}</h1>{desc&&<p className="mt-2 max-w-2xl text-sm leading-6 text-zinc-500">{desc}</p>}</div>}
