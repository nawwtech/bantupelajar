 "use client";
import {useState} from "react";import {createClient} from "../lib/supabase";import {GraduationCap,Loader2,ShieldCheck} from "lucide-react";
export default function Login(){const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function google(){setLoading(true);setError("");const{error}=await createClient().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${window.location.origin}/auth/callback?next=/dashboard`}});if(error){setError(error.message);setLoading(false)}}return <main className="grid min-h-screen place-items-center bg-[#08090d] px-5"><div className="w-full max-w-md"><div className="mb-8 flex justify-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-fuchsia-500"><GraduationCap/></div><span className="text-xl font-bold">Bantu<span className="text-violet-400">Pelajar</span></span></div><div className="glass rounded-[28px] p-7 sm:p-9"><h1 className="text-2xl font-extrabold">Selamat datang 👋</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Masuk dengan Google untuk menyimpan aktivitas belajar kamu.</p><button onClick={google} disabled={loading} className="mt-8 flex w-full items-center justify-center gap-3 rounded-2xl bg-white px-5 py-3.5 font-bold text-black disabled:opacity-60">{loading?<Loader2 className="animate-spin" size={19}/>:<span className="text-lg font-black">G</span>}{loading?"Menghubungkan...":"Lanjutkan dengan Google"}</button>{error&&<p className="mt-4 rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-xs text-red-300">{error}</p>}<div className="mt-7 flex gap-2 text-xs leading-5 text-zinc-600"><ShieldCheck size={16} className="shrink-0"/> BantuPelajar tidak menerima password Google kamu.</div></div></div></main>}
