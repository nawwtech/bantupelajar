"use client";
import {useState} from "react";import {createClient} from "../lib/supabase";import {GraduationCap,Loader2,Mail,ShieldCheck} from "lucide-react";
export default function Login(){const[email,setEmail]=useState('');const[loading,setLoading]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');async function google(){setLoading('google');setError('');const{error}=await createClient().auth.signInWithOAuth({provider:'google',options:{redirectTo:`${window.location.origin}/auth/callback?next=/dashboard`}});if(error){setError(error.message);setLoading('')}}async function emailLogin(){if(!email.trim())return;setLoading('email');setError('');setMessage('');const{error}=await createClient().auth.signInWithOtp({email:email.trim(),options:{emailRedirectTo:`${window.location.origin}/auth/callback?next=/dashboard`}});if(error){setError(error.message)}else setMessage('Link masuk sudah dikirim ke email kamu. Buka email tersebut untuk melanjutkan.');setLoading('')}return <main className="grid min-h-screen place-items-center bg-[#08090d] px-5"><div className="w-full max-w-md"><div className="mb-8 flex justify-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-fuchsia-500"><GraduationCap/></div><span className="text-xl font-bold">Bantu<span className="text-violet-400">Pelajar</span></span></div><div className="glass rounded-[28px] p-7 sm:p-9"><h1 className="text-2xl font-extrabold">Selamat datang 👋</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Pilih cara masuk. Kamu tidak perlu membuat kata sandi untuk masuk lewat email.</p><button onClick={google} disabled={!!loading} className="mt-7 flex w-full items-center justify-center gap-3 rounded-2xl bg-white px-5 py-3.5 font-bold text-black disabled:opacity-60">{loading==='google'?<Loader2 className="animate-spin" size={19}/>:<span className="text-lg font-black">G</span>}{loading==='google'?'Menghubungkan...':'Lanjutkan dengan Google'}</button><div className="my-6 flex items-center gap-3 text-xs text-zinc-700"><span className="h-px flex-1 bg-white/5"/>ATAU<span className="h-px flex-1 bg-white/5"/></div><div className="space-y-3"><label className="text-sm font-semibold text-zinc-300">Email</label><div className="flex items-center gap-2 rounded-2xl border border-white/8 bg-white/[.03] px-4"><Mail size={17} className="text-zinc-600"/><input value={email} onChange={e=>setEmail(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')emailLogin()}} type="email" placeholder="kamu@email.com" className="min-w-0 flex-1 bg-transparent py-3.5 text-sm outline-none"/></div><button onClick={emailLogin} disabled={!!loading||!email.trim()} className="flex w-full items-center justify-center gap-2 rounded-2xl border border-violet-400/20 bg-violet-500/10 px-5 py-3.5 text-sm font-bold text-violet-200 disabled:opacity-40">{loading==='email'?<Loader2 className="animate-spin" size={18}/>:<Mail size={18}/>}Kirim link masuk</button></div>{message&&<p className="mt-4 rounded-xl border border-emerald-400/20 bg-emerald-400/5 p-3 text-xs leading-5 text-emerald-300">{message}</p>}{error&&<p className="mt-4 rounded-xl border border-red-400/20 bg-red-400/5 p-3 text-xs text-red-300">{error}</p>}<div className="mt-7 flex gap-2 text-xs leading-5 text-zinc-600"><ShieldCheck size={16} className="shrink-0"/> Link masuk email dan Google diproses oleh Supabase Auth.</div></div></div></main>}
