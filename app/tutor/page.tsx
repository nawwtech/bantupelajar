"use client";import {useState} from 'react';import {Send,Sparkles} from 'lucide-react';import {AppShell,PageTitle} from '../lib/ui';
export default function Tutor(){const[q,setQ]=useState('');const[a,setA]=useState('');const[e,setE]=useState('');const[l,setL]=useState(false);async function ask(){if(!q.trim()||l)return;setL(true);setA('');setE('');try{const r=await fetch('/tanya',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:q})});const d=await r.json();if(!r.ok)throw Error(d.error||'Gagal');setA(d.answer)}catch(x:any){setE(x.message||'Gagal')}finally{setL(false)}}return <AppShell><PageTitle eyebrow="BELAJAR DENGAN AI" title="AI Tutor" desc="Tanyakan materi, minta contoh, atau minta penjelasan langkah demi langkah."/><div className="glass mt-7 rounded-3xl p-4 sm:p-6"><textarea value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();ask()}}} placeholder="Contoh: Jelaskan hukum Newton 3 dengan bahasa anak SMP..." className="min-h-36 w-full resize-none bg-transparent p-2 text-sm leading-7 outline-none placeholder:text-zinc-700"/><div className="flex justify-end border-t border-white/5 pt-4"><button onClick={ask} disabled={l||!q.trim()} className="inline-flex items-center gap-2 rounded-xl bg-white px-5 py-2.5 text-sm font-bold text-black disabled:opacity-40">{l?'Memikirkan...':<>Tanya <Send size={15}/></>}</button></div></div>{e&&<div className="mt-4 rounded-2xl border border-red-400/20 bg-red-400/5 p-4 text-sm text-red-300">{e}</div>}{a&&<div className="glass mt-5 rounded-3xl p-6"><div className="flex items-center gap-2 text-sm font-bold text-violet-300"><Sparkles size={16}/> BantuPelajar</div><div className="mt-4 whitespace-pre-wrap text-sm leading-7 text-zinc-300">{a}</div></div>}</AppShell>}
